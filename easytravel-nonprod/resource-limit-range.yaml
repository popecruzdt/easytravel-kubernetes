apiVersion: v1
kind: LimitRange
metadata:
  name: resource-limit-range
spec:
  limits:
  - default:
      cpu: "150m"
      memory: "264Mi"
    defaultRequest:
      cpu: "100m"
      memory: "256Mi"
    type: Container
