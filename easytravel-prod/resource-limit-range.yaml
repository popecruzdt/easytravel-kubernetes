apiVersion: v1
kind: LimitRange
metadata:
  name: resource-limit-range
spec:
  limits:
  - default:
      cpu: "250m"
      memory: "350Mi"
    defaultRequest:
      cpu: "250m"
      memory: "350Mi"
    type: Container
